/// App-wide constants.
class AppConstants {
  AppConstants._();

  static const String appName = 'MoTiroong';

  /// Space to leave below scrollable content so it can scroll clear of the
  /// floating liquid-glass navigation bar (bar height + its outer padding).
  static const double bottomBarClearance = 120;
}
